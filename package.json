{
  "name": "tarasstacktodo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "cypress run --spec 'cypress/specs/regression/*' --headless --browser=chrome",
    "cy": "npx cypress open",
    "smoke": "cypress run --spec 'cypress/specs/smoke/*' --headless --browser=chrome",
    "api": "cypress run --spec 'cypress/specs/api/axios/*' --headless --browser=chrome"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.17.2"
  }
}
